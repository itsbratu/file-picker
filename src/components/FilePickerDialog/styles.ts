import { Box } from '@mui/material';
import styled from 'styled-components';

export const DialogActionButtonsStyled = styled(Box)`
  display: flex;
  gap: 15px;
  justify-content: flex-end;
  margin-top: 15px;
`;
